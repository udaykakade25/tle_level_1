create table areas (
  radius number,
  area number
);
/

declare
  r number := 5;
  a number;
begin
  while r <= 9 loop
    a := 3.14 * r * r;
    insert into areas values (r, a);
    r := r + 1;
  end loop;
end;
/
